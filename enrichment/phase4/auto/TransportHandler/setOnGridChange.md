Registers a callback for the high-precision grid timer. The callback receives a grid index, a sample-accurate timestamp offset within the current audio block, and a boolean indicating whether this is the first grid tick in the current playback (useful for resetting sequencer state). The grid must be enabled first with `setEnableGrid()`. Does not fire immediately upon registration.