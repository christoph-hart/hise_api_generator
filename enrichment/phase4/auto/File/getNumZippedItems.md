Returns the number of entries in this ZIP archive. Returns 0 if the file is not a valid ZIP archive or does not exist.

> [!Warning:Parses archive on every call] Parses the archive from disk on every call. Store the result in a variable rather than calling this method in a loop.
