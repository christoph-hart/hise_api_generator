Returns the lower handle value of a range slider. It is useful for filter bands, selection windows, or any two-handle threshold UI.

> [!Warning:$WARNING_TO_BE_REPLACED$] This is valid only in `Range` style; in other styles it reports an error and returns a fallback value.
