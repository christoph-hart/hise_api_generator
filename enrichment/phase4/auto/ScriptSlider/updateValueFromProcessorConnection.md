Pulls the current value from the connected processor/parameter mapping and applies it to the slider. Call it after preset restore or any non-UI parameter mutation path to keep displayed values aligned.

> **Warning:** If you skip this after external processor changes, the slider can display stale values until the next manual interaction.
