Creates a ScriptShader object for GLSL-based rendering in ScriptPanels. Pass a filename to load a fragment shader from the project's shader folder, or an empty string to create a shader without loading a file. Render it from the panel's paint routine with `Graphics.applyShader()` and pass dynamic data via `ScriptShader.setUniformData()`.