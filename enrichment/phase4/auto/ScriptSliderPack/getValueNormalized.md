This inherited scalar helper is intentionally not active for `ScriptSliderPack`.

It just mirrors `getValue()` on scalar components and does not provide meaningful normalised semantics for array data.
