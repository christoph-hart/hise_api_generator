Connects a global receiver modulator to a source inside a GlobalModulatorContainer. The two string parameters identify the container and the source modulator within it.

> **Warning:** Only works on global receiver modulator types (GlobalVoiceStartModulator, GlobalTimeVariantModulator, etc.). Calling this on a regular modulator reports a script error.
