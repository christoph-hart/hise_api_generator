Replaces all occurrences of the search string with the replacement and returns the result. `String.replaceAll()` is an identical alias.

> [!Warning:Replaces all occurrences, not just the first] Unlike JavaScript's `replace`, HISEScript replaces every match in the string. There is no "replace first only" variant.
