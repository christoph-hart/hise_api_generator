{
  "factoryPath": "routing.global_send",
  "nodes": [
    { "id": "in", "label": "Audio In", "type": "input", "importance": 1.0, "description": "Audio signal from local chain (passes through unmodified)" },
    { "id": "push", "label": "Push to Signal Buffer", "type": "process", "importance": 1.0, "description": "Copies audio into shared GlobalRoutingManager Signal buffer with gain scaling (copyWithMultiply)" },
    { "id": "value", "label": "Value", "type": "parameter", "importance": 1.0, "description": "Linear gain multiplier (0..1) applied to the copied signal" },
    { "id": "out", "label": "Audio Out", "type": "output", "importance": 0.8, "description": "Original signal passes through unchanged" }
  ],
  "edges": [
    { "from": "in", "to": "push", "type": "signal", "label": "copy (non-destructive)" },
    { "from": "value", "to": "push", "type": "control", "label": "gain" },
    { "from": "in", "to": "out", "type": "signal", "label": "passthrough" }
  ]
}
